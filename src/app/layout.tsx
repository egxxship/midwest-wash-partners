import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Midwest Wash Partners | Car Wash Development & Management",
  description:
    "Vertically integrated car wash developer across the Midwest. From site selection and capital formation to equipment, service, and ongoing management — we deliver the full lifecycle.",
  keywords: [
    "car wash development",
    "car wash consulting",
    "IBA",
    "in-bay automatic",
    "car wash management",
    "Midwest",
    "Illinois",
  ],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
